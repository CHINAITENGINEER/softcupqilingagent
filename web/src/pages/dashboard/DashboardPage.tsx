import { Area, AreaChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { ArrowRight, Bot, ClipboardCheck, FileKey2, Gauge, Play, Shield } from 'lucide-react'
import { Badge } from '../../components/ui/Badge'
import { Card } from '../../components/ui/Card'
import { MetricCard } from '../../components/ui/MetricCard'
import { SafetyPipeline } from '../../components/safety/SafetyPipeline'
import { api } from '../../api/client'
import { useApiStatus } from '../../hooks/useApiStatus'
import { useDemoResource } from '../../hooks/useDemoResource'
import { useCallback } from 'react'

export default function DashboardPage() { const systemRequest = useCallback(() => api.systemStatus(), []); const ragRequest = useCallback(() => api.ragStatus(), []); const { data: system } = useDemoResource(systemRequest, { status: 'UP', serviceName: 'QilingOS SafeOps Agent', version: 'demo', activeProfiles: [], serverTime: '', agent: { status: 'ONLINE', registeredToolCount: 1 }, approvals: { total: 3, pending: 2, highRiskPending: 1 }, audit: { traceCount: 74, integrityMode: 'ENABLED' }, rag: { enabled: true, retrieverMode: 'vector', vectorStoreProvider: 'in-memory' }, runtime: {} }); const { data: rag } = useDemoResource(ragRequest, { enabled: true, retrieverMode: 'vector', embeddingProvider: 'deterministic', embeddingModel: 'demo', vectorStoreProvider: 'in-memory', milvusUri: '', milvusCollection: 'safeops_knowledge', milvusDimension: 1536, milvusMetricType: 'COSINE', milvusIndexType: 'HNSW', milvusAutoCreateCollection: true, milvusAutoLoadCollection: true, milvusValidateIndex: true, maxResults: 5, contextMaxLength: 8000, snippetMaxLength: 500 }); const { data: health } = useApiStatus(); const trend = [{ h: '08', v: 34 }, { h: '10', v: 51 }, { h: '12', v: 43 }, { h: '14', v: 74 }, { h: '16', v: 62 }, { h: '18', v: 81 }]; return <div className="page"><div className="pagehead"><div><p>SECURE AIOPS COMMAND CENTER</p><h1>Operational overview</h1></div><button className="primary"><Play size={15}/> Start guided operation</button></div><Card className="hero"><div><Badge tone="success"><i/> {health.status === 'UP' ? 'All systems protected' : health.status}</Badge><h2>QilingOS <span>SafeOps Agent</span></h2><p>每一次智能运维，都在策略、审批、验证与不可篡改审计的安全闭环中完成。</p><div className="hero-actions"><button className="primary">Open Agent <ArrowRight size={15}/></button><button className="ghost">View architecture</button></div></div><div className="radar"><div className="radar-ring r1"/><div className="radar-ring r2"/><div className="radar-ring r3"/><div className="radar-sweep"/><span><Shield size={24}/></span><small>TRUST<br/>BOUNDARY</small></div></Card><div className="metrics"><MetricCard icon={<Bot/>} value={String(system.agent.registeredToolCount).padStart(2, '0')} label="Registered tools" note={system.agent.status} tone="cyan"/><MetricCard icon={<ClipboardCheck/>} value={String(system.approvals.pending).padStart(2, '0')} label="Pending approvals" note={`${system.approvals.highRiskPending} high risk`} tone="amber"/><MetricCard icon={<Gauge/>} value={String(system.audit.traceCount)} label="Audit traces" note={system.audit.integrityMode} tone="green"/><MetricCard icon={<FileKey2/>} value={rag.enabled ? 'ON' : 'OFF'} label="RAG retrieval" note={rag.vectorStoreProvider} tone="violet"/></div><div className="grid two"><Card><div className="cardhead"><div><p>SAFETY PIPELINE</p><h3>Closed-loop enforcement</h3></div><Badge tone="success">Protected</Badge></div><SafetyPipeline/><div className="pipeline-note"><Shield size={15}/><span>策略引擎已拦截 14 个越权意图，所有高风险操作均需要人类批准。</span></div></Card><Card><div className="cardhead"><div><p>OPERATION VOLUME</p><h3>Today’s execution signal</h3></div><span className="trend">↗ 18.4%</span></div><div className="chart"><ResponsiveContainer><AreaChart data={trend}><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#49d6ff" stopOpacity=".42"/><stop offset="1" stopColor="#49d6ff" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} stroke="#ffffff0d"/><XAxis dataKey="h" tickLine={false} axisLine={false}/><YAxis hide/><Tooltip/><Area dataKey="v" stroke="#49d6ff" strokeWidth={2} fill="url(#area)"/></AreaChart></ResponsiveContainer></div></Card></div><div className="grid two lower"><Card><div className="cardhead"><div><p>RISK DISTRIBUTION</p><h3>Policy decisions</h3></div><button className="textbtn">Details</button></div><div className="risk"><ResponsiveContainer width="45%"><PieChart><Pie data={[{ v: 62 }, { v: 27 }, { v: 11 }]} dataKey="v" innerRadius={45} outerRadius={68} paddingAngle={4}>{[0, 1, 2].map(index => <Cell key={index} fill={['#4ee0a4', '#ffbd5b', '#ff657d'][index]}/>)}</Pie><text x="50%" y="48%" textAnchor="middle" fill="currentColor" fontSize="20">74</text><text x="50%" y="62%" textAnchor="middle" fill="#8191aa" fontSize="9">EVENTS</text></PieChart></ResponsiveContainer><div className="legend"><span><i className="safe"/> Low risk <b>62%</b></span><span><i className="warn"/> Approval <b>27%</b></span><span><i className="danger-dot"/> Denied <b>11%</b></span></div></div></Card><Card><div className="cardhead"><div><p>VECTOR INTELLIGENCE</p><h3>RAG / Milvus</h3></div><Badge tone="violet">{rag.milvusIndexType}</Badge></div><div className="vector"><div className="nodegrid">{Array.from({ length: 20 }, (_, index) => <i key={index} style={{ animationDelay: `${index * 70}ms` }}/>)}</div><div><strong>{rag.milvusCollection}</strong><span>{rag.vectorStoreProvider} vector store</span><span>{rag.milvusDimension}d · {rag.milvusMetricType} metric</span></div></div></Card></div></div> }
