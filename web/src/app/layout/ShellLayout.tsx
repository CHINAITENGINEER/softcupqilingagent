import { useEffect, useState } from 'react'
import { ChevronRight, ClipboardCheck, FileKey2, Home, Menu, Moon, PanelLeftClose, Settings, Shield, Sparkles, Sun, Bot, BrainCircuit } from 'lucide-react'
import { NavLink, Outlet } from 'react-router-dom'
import { Badge } from '../../components/ui/Badge'
const navigation = [{ to: '/', label: 'Overview', icon: Home, end: true }, { to: '/chat', label: 'Agent Chat', icon: Bot }, { to: '/approvals', label: 'Approval Center', icon: ClipboardCheck }, { to: '/audit', label: 'Audit Trace', icon: FileKey2 }, { to: '/knowledge', label: 'RAG Knowledge', icon: BrainCircuit }, { to: '/settings', label: 'System Settings', icon: Settings }]
export function ShellLayout() { const [collapsed, setCollapsed] = useState(false); const [dark, setDark] = useState(true); useEffect(() => { document.documentElement.dataset.theme = dark ? 'dark' : 'light' }, [dark]); return <div className="shell"><aside className={collapsed ? 'collapsed' : ''}><div className="brand"><span className="brandmark"><Shield size={18}/></span><span className="brandtext">SAFEOPS <small>CONSOLE</small></span><button className="icon collapse" onClick={() => setCollapsed(!collapsed)} aria-label="切换侧栏"><PanelLeftClose size={18}/></button></div><nav>{navigation.map(({ to, label, icon: Icon, end }) => <NavLink end={end} to={to} key={to}><Icon size={18}/><span>{label}</span>{label === 'Approval Center' && <em>2</em>}</NavLink>)}</nav><div className="sidefooter"><div className="pulse"><i/> AGENT ONLINE</div><small>QilingOS · v0.1.0</small></div></aside><main><header><div className="mobile-title"><Menu size={18}/><span>SAFEOPS</span></div><div className="breadcrumb"><span>Command Center</span><ChevronRight size={14}/><strong>Protected environment</strong></div><div className="top-actions"><Badge tone="demo"><Sparkles size={12}/> Demo ready</Badge><button className="icon" onClick={() => setDark(!dark)} aria-label="切换主题">{dark ? <Sun size={17}/> : <Moon size={17}/>}</button><div className="avatar">OP</div></div></header><Outlet/></main></div> }
