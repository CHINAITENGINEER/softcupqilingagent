spring.datasource.url=${SAFEOPS_POSTGRES_URL:jdbc:postgresql://localhost:5432/safeops}
spring.datasource.username=${SAFEOPS_POSTGRES_USER:safeops}
spring.datasource.password=${SAFEOPS_POSTGRES_PASSWORD:safeops}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
